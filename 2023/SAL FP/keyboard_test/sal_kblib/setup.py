from setuptools import setup, find_packages

setup(
    name = "sal_kblib",
    version = "0.0",
    description = "A library that uses the LED functions inside the SAL keyboard",
    author = "Brandon Kirisaki",
    packages = find_packages(),
)
